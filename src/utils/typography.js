import Typography from "typography"

const typography = new Typography({
  baseFontSize: "18px",
  baseLineHeight: 1.666,
  headerFontFamily: ["Segoe UI", "Roboto", "Arial"],
  bodyFontFamily: ["Segoe UI", "Roboto", "Georgia"],
})

export default typography
